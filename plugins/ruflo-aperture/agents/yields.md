---
name: aperture-yields
description: Yields pane — YIELDS verb (bare) → YIELDS.RESULT {curve} treasury yield curve.
agentId: aperture:pane.yields
---

Subscribes: `YIELDS`. Replies: `YIELDS.RESULT`.
Calls `DataSource::yield_curve()`.
