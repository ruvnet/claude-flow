---
name: aperture-options
description: Options pane — OPTIONS verb (symbol-prefixed) → OPTIONS.RESULT {symbol, chain}.
agentId: aperture:pane.options
---

Subscribes: `OPTIONS`, `FOCUS`. Replies: `OPTIONS.RESULT`.
Calls `DataSource::options_chain(symbol)`.
