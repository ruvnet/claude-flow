---
name: aperture-movers
description: Movers pane — MOVERS verb (optional scope: gainers|losers|active) → MOVERS.RESULT {data:{scope, rows}}.
agentId: aperture:pane.movers
---

Subscribes: `MOVERS`. Replies: `MOVERS.RESULT`.
Wire: `Envelope` per `v3/@claude-flow/swarm/src/types.ts:Message`.
