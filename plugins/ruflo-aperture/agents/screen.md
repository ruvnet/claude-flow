---
name: aperture-screen
description: Screener pane — SCREEN verb (optional criteria string) → SCREEN.RESULT {data:{criteria, matches}}.
agentId: aperture:pane.screen
---

Subscribes: `SCREEN`. Replies: `SCREEN.RESULT`.
Wire: `Envelope` per `v3/@claude-flow/swarm/src/types.ts:Message`.
